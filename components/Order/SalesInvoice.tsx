"use client";
import Image from "next/image"; import React, { useRef } from "react";
import logo from "../../public/limiLogo.webp";
import { convertIsoDateToNormal } from "@/lib/convertIsoDatetoNormal";
import { useReactToPrint } from "react-to-print";
export default function SalesInvoice({ order }) { const pendingTaxItem = order?.orderItems?.find( (item) => item.product?.taxMappingStatus === "PENDING_REVIEW" || !item.hsnCode || item.gstRate === null || item.gstRate === undefined ); if (pendingTaxItem) { return ( <div className="rounded-md border border-amber-300 bg-amber-50 p-4 text-amber-900"> Tax mapping is pending review for one or more products. Configure HSN/GST before publishing or invoicing. </div> ); } const shippingCost = order.shippingCost; const invoiceDate = convertIsoDateToNormal(order.createdAt); const subTotal = order.orderItems .reduce((acc, currentItem) => { return acc + currentItem.price * currentItem.quantity; }, 0) .toFixed(2) ?? 0; const total = (parseFloat(subTotal) + parseFloat(order.shippingCost)).toFixed( 2 ); const invoiceRef = useRef<HTMLDivElement>(null); const handlePrint = useReactToPrint({ contentRef: invoiceRef, }); return ( <div className="flex flex-col"> {/* DOWNLOAD BTN */} <div className="flex items-end justify-end mb-8"> <button onClick={handlePrint} type="button" className="inline-flex items-center justify-center px-4 py-3 text-xs font-bold transition-all duration-200 bg-slate-800 text-slate-200 border border-transparent rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 " > Download/Print Invoice </button> </div> {/* INVOICE */} <div ref={invoiceRef}> <div className="max-w-4xl mx-auto border border-gray-500 p-8 rounded-sm text-slate-800 bg-white"> {/* Header */} <div className="flex justify-between border-b border-gray-500 pb-8"> <div className="flex flex-col"> <h2>Bill From:</h2> <p>LimiFood </p> <p>150 Eleign Street</p> <p>Canada</p> <p>sales@limifood.com</p> </div> <Image src={logo} alt="limifood logo" className="w-36 h-24" /> </div> {/* Header End */} <div className="flex justify-between border-b border-gray-500 py-8"> <div className="flex flex-col"> <h2>Bill To:</h2> <p> {order.firstName} {order.lastName} </p> <p> {order?.apartment} {order.streetAddress} </p> <p>{order?.state}</p> <p> {order.city} - {order.country} </p> <p>{order.email}</p> </div> <div className="flex flex-col"> <div className="flex justify-between"> <p>Invoice #</p> <p>{order.orderNumber}</p> </div> <div className="flex justify-between gap-4"> <p>Invoice Date</p> <p>{invoiceDate}</p> </div> <div className="flex justify-between gap-4"> <p>Amount Due</p> <p>${total}</p> </div> </div> </div> <div className="relative overflow-x-auto "> <table className="w-full text-sm text-left rtl:text-right text-gray-500"><thead className="text-xs text-gray-700 uppercase bg-gray-50"> <tr><th scope="col" className="px-6 py-3"> Item Image </th> <th scope="col" className="px-6 py-3"> Item Title </th> <th scope="col" className="px-6 py-3"> Qty </th> <th scope="col" className="px-6 py-3"> HSN/GST </th> <th scope="col" className="px-6 py-3"> Unit Cost </th> <th scope="col" className="px-6 py-3"> Line Total </th></tr> </thead> <tbody> {order.orderItems.map((item, i) => { const itemSubtotal = (item.quantity * item.price).toFixed(2); return ( <tr key={i} className="bg-white border-b" ><th scope="row" className="px-6 py-4 font-medium text-gray-900 whitespace-nowrap" > <Image src={item.imageUrl} width={249} height={249} alt={item.title} className="rounded-xl w-10 h-10" /> </th> <td className="px-6 py-4 truncate">{item.title}</td> <td className="px-6 py-4">{item.quantity}</td> <td className="px-6 py-4"> {item.hsnCode ? `${item.hsnCode} / ${item.gstRate ?? 0}%` : "HSN Not Assigned"} </td> <td className="px-6 py-4">${item.price}</td> <td className="px-6 py-4">${itemSubtotal}</td></tr> ); })} </tbody></table> </div> <div className="flex justify-between border-b border-gray-500 py-8"> <div className="flex flex-col"> <h2>NOTES</h2> <p> {shippingCost == 50 ? "Your Delivery is within 3 days " : shippingCost == 75 ? "Your Delivery is within 2 days" : shippingCost == 90 ? "Your Delivery is within 1 day" : "Thank you for making business with us"} </p> </div> <div className="flex flex-col"> <div className="flex justify-between gap-4"> <p>SubTotal</p> <p>${subTotal}</p> </div> <div className="flex justify-between"> <p>Shipping Cost</p> <p>${order?.shippingCost}</p> </div> <div className="flex justify-between"> <p>Total</p> <p>${total}</p> </div> </div> </div> {/* <div className="flex justify-center items-center pt-8"> <Image src={logo} alt="limifood logo" className="w-36 h-16" /> </div> */} </div> </div> </div> ); } 