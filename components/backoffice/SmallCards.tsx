import React from "react";
import { CheckCheck, Loader2, RefreshCcw, ShoppingCart } from "lucide-react";
export default function SmallCards({ orders }) { const orderData = Array.isArray(orders) ? orders : []; const status = { pending: "PENDING", processing: "PROCESSING", shipping: "SHIPPED", delivering: "DELIVERED", cancelling: "CANCELED", }; function getOrdersCountByStatus(status) { const filteredOrders = orderData.filter( (order) => order.orderStatus === status ); const count = filteredOrders.length.toString().padStart(2, "0"); return count; } const ordersCount = orderData.length.toString().padStart(2, "0"); const pendingOrdersCount = getOrdersCountByStatus(status.pending); const processingOrdersCount = getOrdersCountByStatus(status.processing); const deliveredOrdersCount = getOrdersCountByStatus(status.delivering); const orderStatus = [ { title: "Total Order", number: ordersCount, iconBg: "bg-[#2f7893]", icon: ShoppingCart, }, { title: "Orders Pending", number: pendingOrdersCount, iconBg: "bg-[#7ed9e9]", icon: Loader2, }, { title: "Order Processing", number: processingOrdersCount, iconBg: "bg-[#ffd85b]", icon: RefreshCcw, }, { title: "Orders Delivered", number: deliveredOrdersCount, iconBg: "bg-[#ff75b6]", icon: CheckCheck, }, ]; return ( <div className="grid gap-6 md:grid-cols-2 min-[1440px]:grid-cols-4"> {orderStatus.map((data, i) => { const Icon = data.icon; return ( <article className="liquid-card single-glass min-h-28 p-6 text-[#103c55]" key={i} > <div className="relative z-10 flex items-center gap-5"> <div className="liquid-circle h-14 w-14 shrink-0 text-[#15516d]"> <Icon className="h-6 w-6" /> </div> <div className="min-w-0"> <p className="truncate text-base font-medium text-[#2f6d85]"> {data.title} </p> <h3 className="mt-2 text-3xl font-semibold text-[#0f3c54]"> {data.number} </h3> </div> </div> </article> ); })} </div> );
}
