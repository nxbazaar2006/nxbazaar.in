import { ChevronRight } from "lucide-react"; import Link from "next/link"; import React from "react"; import Breadcrumb from "./Breadcrumb"; import Sorting from "./Sorting"; import Filters from "./Filters"; import FilteredProducts from "./FilteredProducts";
export default function FilterComponent({ category, products, selectedSubCategorySlug, }) { const { title, slug } = category; const productCount = products.length; const subCategories = category.subCategories ?? []; return ( <div> <div className="liquid-card space-y-6 px-4 py-8 text-slate-900"> <Breadcrumb title={title} resultCount={productCount} /> <Sorting isSearch={category?.isSearch} title={title} slug={slug} /> {subCategories.length > 0 && ( <div className="flex flex-wrap gap-2"> <Link href={`/category/${slug}`} className={ selectedSubCategorySlug ? "rounded-full border border-slate-300 px-4 py-2 text-sm" : "rounded-full bg-slate-900 px-4 py-2 text-sm text-white" } > All </Link> {subCategories.map((subCategory) => ( <Link key={subCategory.id} href={`/category/${slug}?subCategory=${subCategory.slug}`} className={ selectedSubCategorySlug === subCategory.slug ? "rounded-full bg-slate-900 px-4 py-2 text-sm text-white" : "rounded-full border border-slate-300 px-4 py-2 text-sm" } > {subCategory.title} </Link> ))} </div> )} </div> <div className="grid grid-cols-12 py-8 gap-4"> <div className="col-span-3"> <Filters slug={slug} isSearch={category?.isSearch} /> </div> <div className="col-span-9"> <FilteredProducts isSearch={category?.isSearch} productCount={productCount} products={products} /> </div> </div> </div> ); } 
